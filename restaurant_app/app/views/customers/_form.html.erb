<%= form_for @customer do |f| %>

  <%= f.label :name %>
  <%= f.text_field :name %>

  <%= f.label :table %>
  <%= f.number_field :table, min: 1, step: 1 %>

  <%= f.label :party_size %>
  <%= f.number_field :party_size, min: 1, step: 1 %>
  <br>
  <%= f.submit %>
<% end %>
