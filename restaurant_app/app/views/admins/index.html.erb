<!-- THIS IS ADMIN PAGE. -->
<div class="container">
  <h1 class="stitle">Admin Panel for <%= User.find(session[:current_user_id]).name %></h1>
  <p class="stitle">
    <%= link_to "Logout", sessions_path, method: :delete %>
   </p>

  <h3><%= link_to "Users", users_path %></h3>
  <h3><%= link_to "Menu Items", mitems_path %></h3>
  <h3><%= link_to "Customers", customers_path %></h3>

</div>
